<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Roleta Caótica</title>
    <style>
      body { font-family: Arial, sans-serif; background-color: #2c003e; /* Fundo
      roxo escuro */ background-image:
      url('https://www.transparenttextures.com/patterns/stardust.png'); /*
      Textura de estrelas */ display: flex; justify-content: center;
      align-items: center; height: 100vh; margin: 0; color: #fff; /* Texto
      branco para contraste */ } .container { background-color: rgba(255, 255,
      255, 0.1); /* Fundo branco translúcido */ padding: 20px; border-radius:
      10px; box-shadow: 0 0 20px rgba(255, 255, 255, 0.2); /* Sombra branca
      suave */ text-align: center; } h1 { color: #e0b3ff; /* Título roxo claro
      */ } p { font-size: 1.5em; color: #d1a3ff; /* Texto roxo claro */ }
      .button { background-color: #6a0dad; /* Fundo roxo */ color: #fff; /*
      Texto branco */ border: none; padding: 10px 20px; font-size: 1em; cursor:
      pointer; }
    </style>
  </head>
  <body>
    <div class='container'>
      <h1>Roleta Caótica</h1>
      <p id='message'>Clique no botão para rolar o dado!</p>
      <button class='button' onclick='rollDice()'>Rolar Dado</button>
    </div>
    <script>
      async function rollDice() { try { const response = await
      fetch('/events/roll'); const message = await response.text();
      document.getElementById('message').innerText = message; } catch (error) {
      console.error('Error:', error);
      document.getElementById('message').innerText = 'Erro ao rolar o dado.'; }
      }
    </script>
  </body>
</html>